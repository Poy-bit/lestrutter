#version 330 core
layout (location = 0) in vec3 aPos;
  
out vec4 vertexColor;
uniform mat4 cammat;
uniform mat4 projection;

void main() {
    gl_Position = projection * cammat * vec4(aPos, 1.0);
    vertexColor = vec4(0.5, 0.25, 0.0, 1.0);
}